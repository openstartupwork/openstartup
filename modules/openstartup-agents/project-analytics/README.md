# project-analytics

Progress tracking and milestone insights

## Quick Start

import Agent from '@openstartup/project-analytics'

const agent = new Agent({ id: 'agent1', name: 'project-analytics', workspaceId: 'ws1', walletAddress: 'YourSolanaWallet' })
await agent.initialize()

## Handlers

progress-report velocity-tracking milestone-analysis status-update
