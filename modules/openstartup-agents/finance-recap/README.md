# finance-recap

Financial recap, burn rate, budget forecasting

## Quick Start

import Agent from '@openstartup/finance-recap'

const agent = new Agent({ id: 'agent1', name: 'finance-recap', workspaceId: 'ws1', walletAddress: 'YourSolanaWallet' })
await agent.initialize()

## Handlers

budget-summary burn-rate expense-report forecast
