# design-review

UI/UX and accessibility audit

## Quick Start

import Agent from '@openstartup/design-review'

const agent = new Agent({ id: 'agent1', name: 'design-review', workspaceId: 'ws1', walletAddress: 'YourSolanaWallet' })
await agent.initialize()

## Handlers

ui-review accessibility-audit design-system component-spec
