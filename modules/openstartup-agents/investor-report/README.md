# investor-report

Investor updates, KPI and funding status

## Quick Start

import Agent from '@openstartup/investor-report'

const agent = new Agent({ id: 'agent1', name: 'investor-report', workspaceId: 'ws1', walletAddress: 'YourSolanaWallet' })
await agent.initialize()

## Handlers

investor-update pitch-deck kpi-report fundraising-tracker
